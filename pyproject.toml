[project]
name = "task-manager-backend"
version = "0.1.0"
description = "Backend for the Task Manager app"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.139.2",
    "pydantic>=2.13.4",
]
package-dir = {"" = "src"}
